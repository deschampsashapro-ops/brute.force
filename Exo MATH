import math

a = 0
b = 1
precision = 0.001

while b - a > precision:
    m = (a + b) / 2
    if math.exp(m) < 2:
        a = m
    else:
        b = m

solution = (a + b) / 2
print("Solution approchée :", solution)